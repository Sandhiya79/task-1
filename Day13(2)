#write a python program that matches word containg 'ab'

import re
def text_match(text):
        patterns = '\w*ab.\w*'
        if re.search(patterns,  text):
                return 'Found a match!'
        else:
                return('Not matched!')

print(text_match("abitha"))
print(text_match("meena"))
